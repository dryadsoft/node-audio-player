import React from "react";

const Loading = () => {
  return (
    <div className="container flex flex-col w-screen h-screen mx-auto items-center justify-center bg-slate-700 text-gray-300">
      loading
    </div>
  );
};

export default Loading;
